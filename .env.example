# For local development with `wrangler dev`, create a .dev.vars file:
#
# SNIPEIT_URL=https://your-snipeit-instance.com
# SNIPEIT_API_TOKEN=your-api-token-here
#
# For production, use Cloudflare Secrets Store (see wrangler.toml).
# Run `npm run setup-secrets` for guided setup.
